from django.urls import path, include # type: ignore
from django.views.generic import TemplateView  # type: ignore # Добавьте этот импорт
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView

urlpatterns = [
    # Вариант 1: Простое приветствие
    path('', TemplateView.as_view(template_name='home.html'), name='home'),
    
    # Вариант 2: Перенаправление на API
    # path('', RedirectView.as_view(url='/api/')),
    
    # Остальные ваши маршруты
    path('admin/', admin.site.urls), # type: ignore
    path('api/', include('api.urls')),
    path('api/auth/token/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    path('api/auth/token/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
]